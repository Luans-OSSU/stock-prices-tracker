import {StyleSheet} from "react-native";

export default style = StyleSheet.create({
    headerContainer: {
        display: "flex",
        marginTop: 55,
        alignItems: "center"
    },
    header: {
        fontWeight: "bold",
        fontSize: 20
    }
})